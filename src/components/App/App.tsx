import * as React from "react";
import { AppBar } from "../AppBar";

class App extends React.Component {
  public render() {
    return (
      <div>
        <AppBar />
      </div>
    );
  }
}

export default App;
